# Backend API base URL — update when backend is ready
VITE_API_URL=http://localhost:5000/api/v1

# 24/7 Emergency maternity line — replace with real number
VITE_EMERGENCY_PHONE=+254 700 000 000

# Google Maps JavaScript API key — generate from Google Cloud Console
VITE_GOOGLE_MAPS_KEY=
